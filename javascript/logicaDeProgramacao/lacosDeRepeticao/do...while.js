// Diferente do while, o do...while executa o bloco de código pelo menos uma vez antes de verificar a condição.

// Exemplo: Contagem de 0 a 10
contador = 0;
do {
    console.log(contador);
    contador++;
} while (contador <= 10);
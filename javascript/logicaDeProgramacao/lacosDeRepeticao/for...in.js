// O laço for...in é usado para iterar sobre as propriedades de um objeto ou os índices de um array.

// Exemplo: Iteração sobre índices de um array
const objetos = ['redstone', 'diamond sword', 'golden apple'];
for (let index in objetos) {
    console.log(index, objetos[index]);
}
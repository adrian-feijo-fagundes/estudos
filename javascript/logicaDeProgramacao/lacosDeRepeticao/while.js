// No laço while, o teste lógico é feito no início. Ele executa o bloco de código enquanto a condição é true. É ideal quando o número de iterações não é conhecido previamente.

// Exemplo: Contagem de 0 a 10
let contador = 0;
while (contador <= 10) {
    console.log(contador);
    contador++;
}
